<%- include('../partials/header') %>

<table id="list">
  <thead>
    <tr>
      <th>Goals</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% goals.forEach(function(m) { %>
      <tr>
        <td><%= g.title %></td>
        <td><%= g.date %></td>
        <td><a href="/Goals/<%= m._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>